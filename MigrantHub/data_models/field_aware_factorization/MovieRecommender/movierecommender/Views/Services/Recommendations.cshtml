@using servicerecommender.Models
@model IEnumerable<Service>
@{
    ViewBag.Title = "Recommendations";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1><span class="glyphicon glyphicon-film"></span> Service Recommender</h1>

<hr>
<h3>Trending Services</h3>

<div class="row">
    @if (Model != null)
    {
        foreach (var service in Model)
        {
            <div class="col-sm-6">
                <div class="card">
                    <div class="card-block">
                        <h3 class="card-title">@service.ServiceName</h3>
                        <a href="@Url.Action("Watch", "Services")" class="btn btn-primary">Play Service</a>
                    </div>
                </div>
            </div>
        }
    }
    else
    {
        <h2>Something went wrong! please check the error log.</h2>
    }
</div>
<hr />
<button type="button" class="btn breadcrumb" id="select" onclick="location.href='@Url.Action("Profiles", "Services")'">
    <span class="glyphicon glyphicon-ok "></span> This is fun, let me try again!
</button>
